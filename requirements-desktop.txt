# Desktop client: embedded Vue UI (default) or classic Tk (--legacy-tk)
# Core API + web shell
requests>=2.28.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
jinja2>=3.1.0
python-multipart>=0.0.6

# Native window around the same template as web_platform
pywebview>=4.4.0
